{
  "name": "{%= name %}",
  "version": "{%= version %}",
  "description": "{%= description %}",
  "dependencies": {
    "commander": "~1.1.1"
  },
  "devDependencies": {
    "mocha": "~1.8.1",
    "should": "~1.1.0",
    "mocha-lcov-reporter": "~0.0.1",
    "coveralls": "~2.0.7",
    "blanket": "~1.1.4"
  },
  "scripts": {
    "test": "make test",
    "blanket": {
      "pattern": "//^((?!(node_modules|test)).)*$/"
    }
  },
  "main": "./index.js",
  "bin": {
    "{%= name %}": "bin/{%= name %}"
  }
}
